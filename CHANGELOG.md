# 更新日志

## v1.1.1 — 2026-09-06

修复普通裁判文书 OCR 后主要只能填入执行依据，而申请执行人、被执行人及执行请求未能关联的问题。本记录在下列验证完成后整理；保留本地处理、来源核对和全部字段可编辑的边界。

### 改进与修复

- 新增当事人段落和裁判主文解析。按主文明示的收付款关系匹配执行角色，不再仅依赖材料恰好含有“申请执行人／被执行人”标签，也不把原告固定当作申请人。支持常见反诉／原审身份括号、OCR 字间空格及跨页条款。
- 关联自然人身份证明和单位营业执照。按姓名／名称补齐证件号码、主体类型、地址等；新增“身份证明归属”选择，可对单独身份材料指定申请人或被执行人。同名但号码不一致、姓名不符、多主体等不强行串填；排除代理人、法定代表人的个人信息干扰。
- 从最终主文提取确定的单项本金类给付，并整理可编辑的执行请求草稿。不使用诉请或事实陈述金额，不把利息基数、费用或合计当本金，不自动新增利息或费用。多个本金类给付即使同额也不合并、不自动相加。
- 一批材料全部读取后统一匹配；简单二审维持原判须对应一审案号与当事人。改判、撤销、二审读取失败及复杂给付关系显示人工核对提示，不混用旧给付内容。
- 保留 OCR 文件名、页码、原文和参考分数；跨页、跨材料匹配留有对应摘录。继续保护手工输入和主动清空，增加识别期间改名后的字段归属检查，避免旧主体信息自动附到新姓名下。
- 应用、包版本、模板标记、JSON／Word／ZIP 下载文件名及本地运行包更新至 v1.1.1；草稿结构仍为 v2，兼容旧版。未新增依赖、云端 OCR、外部模型、上传接口或收费服务。

### 验收结果

全部测试材料为虚构软件验收样本，证件号码为测试占位值，不使用真实案件、身份证或营业执照。

| 检查 | 结果与证据 |
| --- | --- |
| 自动测试 | 55 项通过：原 36 项回归及新增 19 项上下文识别测试。覆盖四组填入、反向给付、分页空格、公司证照、身份冲突、低分、代理人隔离、主文／诉称隔离、多项本金、二审对应、手工改名与清空 |
| 构建与依赖 | TypeScript/Vite 生产构建通过；生产依赖漏洞检查 0；依赖版本未变化 |
| 普通扫描文书＋身份证明 | 无隐藏文字层的两页扫描 PDF 加 PNG 身份材料，自动填入 15 项，覆盖执行依据、双方信息和请求；识别到第二页明确生效日期，主文本金 100000.00 元，不误用诉请 150000 元 |
| 反向给付 | 反诉扫描样本“原告张三向被告李四返还借款80000元”，正确填入申请人李四、被执行人张三、本金 80000.00 元及对应请求 |
| 公司与营业执照 | 扫描判决加营业执照图片自动填入 12 项，包含单位名称、统一社会信用代码、住所、50000.00 元本金；未把法定代表人身份证号码填作公司代码 |
| 身份归属与取消 | 单独身份证默认不指定角色；选择申请执行人后填入 4 项。赵六证件不串入张三案件；取消后已有字段保留，重试后正常完成 |
| 编辑保护与恢复 | 改名“人工核对测试名”、主动清空对方地址、改写请求后重复识别，均保持手工值。实际下载并恢复 JSON 后仍保留这些值、手工标记及原 OCR 来源，原文件只恢复索引，确认需重做 |
| 下游与真实导出 | OCR 的 100000.00 元进入只整理本金模式；网页预览、实际 Word、ZIP 中 Word／JSON、剪贴板一致。默认 ZIP 5 项，无原文件；实际 Word 三页逐页核对中文、主体信息、金额、法院和分页通过 |
| 页面 | 本机页面标题为 v1.1.1，正文与关键控件正常，无框架错误层；内置浏览器正常流程控制台无警告／错误。桌面及 375×844 窄屏检查通过，五页正文均无横向溢出；新增归属选择及来源文字可读 |
| 本地运行包 | 完整 ZIP 共 231 项、25293303 字节，逐项解压后与输入字节比对通过，包含当前生产网页及 OCR 资源 |

环境：macOS；Node.js 24.19.0 和 25.8.1；Codex 内置浏览器本机生产页面；实际下载、恢复和复制另用已安装的 Playwright 1.62.1／Chromium 147.0.7727.15 验证。Word 使用独立渲染环境核对，没有修改系统字体或用户浏览器配置。

### 验证路径与已知限制

- 本轮内置浏览器显示已发起下载，但未得到下载事件或实际文件；因此不宣称该下载路径通过。独立 Chromium 已实际取得 JSON、DOCX、ZIP，并完成恢复、内容与金额检查。独立测试脚本首次窄屏阶段使用了桌面侧栏定位，因窄屏隐藏侧栏而超时；窄屏检查改用页面现有的五步导航，在内置浏览器完成，不据此更改应用。
- 这是本地 OCR 加限定规则的字段关联，不是全面法律文书理解。复杂版式、低清图、手写、误识别、特殊主文表达仍可能漏填；请求草稿中的 OCR 标点和措辞、未履行范围均须人工核对。
- 多人、多方向、连带／附条件责任、分期、抵扣、多个本金类项目及二审改判等不自动作单人单本金结论；不判断文书效力、真实身份、管辖或法院是否受理。
- 本轮重新验证重点为三类裁判扫描样本及身份图片。其他格式和长 PDF 的旧版验收记录保留在 v1.1.0 部分；未重复验收所有编码、真实案件准确率、Windows、Safari／Firefox、Vercel 线上完整流程或断网后的全部系统行为。
- 需求文件、独立演示、设计图、参考资料、原始案件材料及临时验收附件没有纳入 Git 提交，也未被删除。

## v1.1.0 — 2026-09-06

本版本重点是材料 OCR、案件信息自动填入和用户编辑保护。本记录在本机功能、构建及导出验收完成后整理；“通过”的范围如下，不代表真实案件准确率或法律审核结论。

### 新增

- 接入 GitHub 上的 Tesseract.js 7.0.0，提供简体中文和英文印刷文字 OCR，不使用云端 OCR、付费 API 或外部大模型。
- PDF.js 逐页将 PDF 渲染为图像后 OCR，覆盖文本 PDF、扫描 PDF 和混合页面，保留实际页码。单份最多 50 页。
- 支持 PNG、JPG/JPEG、WebP、BMP、TIF/TIFF OCR；TIFF 支持逐页处理。每份最多 20 MB，每张图片最多 2500 万像素、单边 16000 像素。
- 识别出的明确案件字段自动填入空白处，显示原始摘录、文件名、位置及 OCR 参考分数。同批材料先收集候选，再判断字段冲突。
- 增加进度、取消、加载和识别超时处理。取消本批次不自动填入本批结果；错误不会覆盖已有案件字段。
- OCR 程序、全部引擎变体、中英文模型、TIFF 解码器和 PDF 辅助资源由构建步骤从锁定依赖复制，使用同源路径。新增生产内容安全策略。
- 新增完整本地运行 ZIP，包含静态网页、OCR 资源和仅监听本机的静态启动器。

### 自动填入规则与兼容

- 所有字段仍可编辑或清空。手动修改过的字段，包括主动清空，以及识别期间新输入的内容，不被重复识别覆盖。
- OCR 行参考分数至少 75/100、格式有效且没有同字段不同值时才自动填入。该分数不等于识别准确率；低分、冲突及上下文不足的结果留作候选。
- 金额的等价格式如 100000 与 100000.00 不视为冲突。来源中的原识别值会在用户修改后保留。
- 不从原告、被告推断执行角色或管辖，不从普通日期推定生效；无明确归属的证件内容由用户对应填写。诉称、事实部分的金额不直接作为执行金额结论。
- 草稿格式仍为 v2，增加识别方式、参考分数、原识别值和手工修改标记；兼容没有新增标记的 v1.0 草稿和旧演示 v1 草稿。恢复后仍需重新确认。
- DOCX/TXT 保留正文读取；Word 内嵌图片须先导出 PDF 或另存图片。旧 DOC、HEIC、GIF、SVG 等须转换到支持格式。
- 应用、下载文件名及文书模板版本更新为 v1.1。原有统一金额、核对、导出门槛及由用户在官方渠道提交的边界保持不变。
- 不再生成单文件 HTML。`build:single` 会说明迁移到 `build:offline`，不覆盖已有 v1.0 文件；v1.1 使用完整静态目录或本地运行包。

### 验收结果

| 检查 | 结果与证据 |
| --- | --- |
| 自动测试 | 36 项通过。覆盖原有计算/草稿/导出规则、OCR 规范化、同批冲突、低分和角色保护、手动修改与清空、长字段恢复、文件边界、取消、加载失败及过期响应 |
| 构建与依赖 | TypeScript/Vite 生产构建、本地运行包构建通过；生产依赖漏洞检查为 0 |
| PDF OCR | 全虚构的两页扫描、两页文本、两页混合 PDF 均完成 OCR；扫描样本得到 12 项字段，包括第二页的生效日期和 100000.00 元本金；20 页扫描 PDF 完成全部页面 |
| 图片 OCR | PNG、JPG、WebP、BMP、两页 LZW TIFF 和 Group 4 黑白 TIFF 均取得文字及字段候选；不同编码的字段数不同，未据此宣称真实准确率 |
| 编辑与取消 | 自动姓名改成手工值后保留；地址通过键盘主动清空后不被回填；识别过程中把本金改为 125000，完成后仍为该值；取消后可重试，已有字段保持不变 |
| 草稿恢复 | 实际下载并恢复 JSON，手工姓名、主动清空和原 OCR 来源保留；只恢复原文件索引，不伪装为原件，确认状态清除 |
| 错误路径 | 加密 PDF、51 页 PDF、空白 PDF/PNG、程序资源不可用均显示明确错误；伪装图片、无效尺寸、错误草稿由自动测试覆盖 |
| 下游一致性 | 修改后的 125000 元进入试算；4.2%、2026-01-01 至 2026-06-30、含首尾 181 天，得到利息 2603.42 元、合计 127603.42 元；页面、实际 Word、ZIP、JSON 和实际复制的合计一致 |
| 实际导出 | Word 与 ZIP 均通过浏览器下载；默认 ZIP 的 5 项内容和金额/法院/手工姓名已核对，未包含原件；实际 Word 三页逐页检查中文、内容和分页 |
| 页面与布局 | 本机生产页面身份正确、非空白，无框架错误层；最终正常流程控制台无警告或错误；桌面及 375×844 窄屏已检查，五页正文无横向溢出 |
| 本地运行包 | ZIP 内容逐项解压比对通过，解压启动后完成 OCR 及下游流程；本机启动器 GET 成功，POST 返回 405 |
| 隐私边界 | 受测静态服务器共 62 次 GET，请求体合计 0 字节，URL 仅为页面和静态资源；包含 1 次主动模拟资源不可用的 503。未在这些请求中出现案件文件名或内容。源代码无案件上传接口，生产 CSP 限制到同源资源 |

环境：macOS、Codex 内置浏览器，本机生产构建与完整运行包；自动测试使用 Node.js 24.19.0、25.8.1。所有测试材料均为软件验收专用虚构内容，没有使用真实案件资料。

### 已知限制与未覆盖范围

- OCR 是文字识别加明确标签匹配，不是法律文书理解、真实性验证或法院受理判断。复杂版式、手写、模糊照片及印章遮挡可能漏读或误读；每项仍需核对原件。
- Windows、Safari/Firefox 实机、其他 TIFF 编码/方向标签和 Vercel 线上完整功能回归未纳入本次通过范围。
- 本地运行包已包含运行所需 OCR 资源，但未通过切断系统网络验证所有浏览器行为；静态服务日志不是全系统抓包，不能据此约束扩展或系统自身的通信。
- 语言模型可缓存至 IndexedDB，案件原件与识别全文只在页面内存中。下载的草稿及剪贴板内容为明文，请自行保管。
- 申请书仍是未经法律专业审核的核对草稿，不提供自动登录、验证码处理、法院内部状态查询或自动提交。

## v1.0

建立五步材料准备流程、本地 PDF/DOCX/TXT 文字读取与候选来源、统一金额试算、JSON 草稿恢复、真实 Word/ZIP 导出及官方填报复制页。初版不含图片 OCR。
